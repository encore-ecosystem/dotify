actions:
  - procedure: git.clone
    info: Clonning yay repository
    url: https://aur.archlinux.org/yay.git

  - procedure: shell.run
    info: Install yay
    command: makepkg
    args: ["-si", "-D", "yay"]
    skip_condition: "yay -V"

depends:
  - git
  - build
