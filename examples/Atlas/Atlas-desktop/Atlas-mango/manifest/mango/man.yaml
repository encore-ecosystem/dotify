actions:
  - procedure: aur.install
    info: "Installing mango"
    packages:
      - mangowm-git

  - procedure: os.symlink
    info: "Adding symlink for mango config"
    src: "files/mango"
    dst: "{{ user.config }}"
