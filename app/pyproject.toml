[project]
name = "dotify-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotify_lib"
]

[tool.uv.sources]
dotify_lib = { workspace = true }

[project.scripts]
dotify = "cli:main"

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"
